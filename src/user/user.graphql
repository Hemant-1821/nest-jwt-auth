type User {
    id: ID!
    email: String!
    password: String!
}

type Query {
    me: User
}

type Mutation {
    login(email: String, password: String): String
    Signup(email: String, password: String): User
}

